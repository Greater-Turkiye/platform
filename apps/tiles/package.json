{
  "name": "gt-tiles",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker that serves the project's own vector basemap: it reads byte ranges out of a PMTiles archive and hands MapLibre one tile per request",
  "license": "MIT",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "pmtiles": "4.5.0"
  },
  "devDependencies": {
    "wrangler": "4.132.0"
  }
}
