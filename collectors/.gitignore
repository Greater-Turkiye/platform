.venv/
__pycache__/
*.egg-info/
.pytest_cache/
.ruff_cache/
build/
dist/

# Local review-queue runs: the batch is a build product and the ledger belongs on the
# collector-state branch, never on main (see state/README.md).
queue/
state/seen.jsonl
